# 星露谷物语风格个人品牌网站全量需求与交互技术规范文档 (PRD)

## 一、 项目背景与核心设计理念

### 1.1 项目定位
本网站是一个极具沉浸感、情感化设计的**星露谷物语（Stardew Valley）像素风格个人主页**。该项目旨在作为申请 **AI 产品经理（AI PM）** 岗位的核心作品集载体。网站不直接罗列枯燥的履历，而是通过高度还原的游戏交互、任务驱动的探索流程、以及"元数据式"的隐喻，向面试官展示作者优秀的用户体验（UX）设计能力、状态机逻辑思维、需求抽象能力以及对生活的热爱。

### 1.2 核心设计原则
1. **沉浸式还原 (Immersive Pixel UI)**：严格采用像素化审美，包括字体、边框、光标和色彩搭配，消除一切现代网页的冰冷感。
2. **PC端绝对优先 (PC-First Mode)**：禁止任何浏览器原生滚动条，锁定 100vw 和 100vh 全屏视口，通过鼠标滚轮、点击以及内部状态机进行平滑转场，不进行手机端适配。
3. **自适应不变形 (Aspect-Ratio Protection)**：针对所有视觉素材采用严格的边界限制与等比例缩放（`object-fit: contain`），确保无论访客分辨率如何，像素人像和道具均不拉伸变形。
4. **透明热区与可见资产分离 (Separation of Hotspot & Assets)**：场景背景内置基本静态元素，可收集物品以绝对定位悬浮其上，具备动态滤镜反馈，点击后完美融入底层 JS 状态机。

### 1.3 技术栈选型
本项目采用 **纯原生前端技术栈（Vanilla HTML + CSS + JavaScript）**，理由如下：
- 无框架依赖，一个 `index.html` 即可承载全部逻辑，部署成本为零（GitHub Pages / 静态服务器直传）。
- 状态机逻辑简单清晰，无需引入 React/Vue 的状态管理开销。
- 像素风格的 DOM 操作（弹窗、淡入淡出、物品栏）天然适合原生 JS 直接操控。
- 对面试官而言，原生实现更能展示对 Web 底层的掌控力。

**目录结构**：
```
/
├── index.html          # 单页入口，内联 CSS 与 JS（或拆分）
├── assets/             # 全部图片素材
└── README.md           # 项目说明
```

> 如后续体量膨胀，可将 CSS/JS 拆分为独立文件，但初期建议单文件便于部署。

---

## 二、 全局视觉与技术规范

### 2.1 视觉资产命名规范 (Asset Mapping)
所有素材统一放置于项目根目录的 `assets/` 文件夹下，严格执行以下命名：

| 素材类别 | 文件命名 | 描述与推荐尺寸规范 |
| :--- | :--- | :--- |
| **场景背景** | `bg_office.png` | Stage 1 办公室背景（内置抓狂王高达），16:9，全屏铺满 |
| | `bg_spring.png` | 春季鹈鹕镇背景（内置垃圾桶环境），16:9，全屏铺满 |
| | `bg_summer.png` | 夏季海滩背景（内置码头/鱼竿环境），16:9，全屏铺满 |
| | `bg_autumn.png` | 秋季祭典背景（内置广场环境），16:9，全屏铺满 |
| | `bg_winter.png` | 冬季雪景背景（内置营火环境），16:9，全屏铺满 |
| | `bg_final.png` | 终章晴空农场背景，16:9，全屏铺满 |
| **NPC图像** | `wang_spring.png` | 春季王高达像素小人（拾荒姿势），透明背景 PNG，等比缩放 |
| | `wang_summer.png` | 夏季王高达像素小人（垂钓姿势），透明背景 PNG，等比缩放 |
| | `wang_autumn.png` | 秋季王高达像素小人（祭典逛街姿势），透明背景 PNG，等比缩放 |
| | `wang_winter.png` | 冬季王高达像素小人（缩在火堆旁姿势），透明背景 PNG，等比缩放 |
| | `wang_final.png` | 终章王高达像素小人（农场主站姿），透明背景 PNG，建议≥400px 高 |
| **交互道具** | `icon_item1.png` | 场景可拾取物/物品栏亮起：被丢弃的草稿（春），透明背景 PNG |
| | `icon_item2.png` | 场景可拾取物/物品栏亮起：闪烁的贝壳碎片（夏），透明背景 PNG |
| | `icon_item3.png` | 场景可拾取物/物品栏亮起：幸运紫色短裤（秋），透明背景 PNG |
| | `icon_item4.png` | 场景可拾取物/物品栏亮起：残存的种子（冬），透明背景 PNG |
| **UI 与系统**| `box_dialog.png` | 星露谷经典黄色木纹对话框底图，九宫格/自适应拉伸 |
| | `cursor_stardew.png`| 星露谷风格箭头鼠标指针（32x32 像素，透明背景，必须降采样） |
| **音效（可选）**| `sfx_click.mp3` | UI 点击音效 |
| | `sfx_pickup.mp3` | 道具拾取音效 |
| | `sfx_transition.mp3`| 转场切换音效 |
| | `sfx_letter.mp3` | 信件展开音效 |

### 2.2 全局 CSS 基础配置
- 通过 `@import` 引入像素中文字体 **Zpix（最像素）**。注意：`VT323` 仅支持拉丁字符，不可用于中文显示。备选方案：`https://cdn.jsdelivr.net/npm/zpix@3.1.4/dist/Zpix.woff2`。
- 设定全局 CSS 变量管理星露谷经典色系：
  ```css
  :root {
    --color-bg-dark: #2d1b0e;        /* 深棕背景 */
    --color-border: #e8c170;          /* 浅黄木纹边框 */
    --color-highlight: #ffd700;       /* 金色高亮文字 */
    --color-text: #f5e6d3;            /* 暖白正文 */
    --color-inventory-empty: rgba(255,255,255,0.15); /* 物品栏空格 */
    --color-inventory-filled: rgba(255,215,0,0.4);   /* 物品栏已收集 */
    --font-pixel: 'Zpix', 'PixelMplus12', monospace;
  }
  ```
- 所有交互元素（`button`, `a`, `.hotspot`）和全局背景必须强制应用自定义鼠标指针 `cursor_stardew.png`：
  ```css
  * { cursor: url('assets/cursor_stardew.png') 2 2, auto; }
  ```
- NPC 图片（`img.npc`）必须设置 `max-height: 55vh` 并配合 `object-fit: contain` 防止变形。
- 物品道具（`img.item`）必须设置 `max-height: 8vh` 并配合 `object-fit: contain`。
- **禁止原生滚动条**：`body { overflow: hidden; margin: 0; }`
- 所有背景图使用 `background-size: cover; background-position: center;` 铺满全屏。

---

## 三、 核心用户旅程与状态机逻辑

网页底层通过 JavaScript 的 `GlobalStateManager` 对象维护整站的状态流转。

```javascript
const GlobalStateManager = {
  currentStage: 'OFFICE', // 可选值: 'OFFICE', 'SEASONS', 'ENDING'
  currentSeason: 'SPRING', // 可选值: 'SPRING', 'SUMMER', 'AUTUMN', 'WINTER'
  inventory: {
    item1: false, // 春: 被丢弃的草稿
    item2: false, // 夏: 闪烁的贝壳碎片
    item3: false, // 秋: 幸运紫色短裤
    item4: false  // 冬: 残存的种子
  },
  dialogueHistory: {
    SPRING: false,
    SUMMER: false,
    AUTUMN: false,
    WINTER: false
  },
  lewisClickCount: 0,       // 秋季捉迷藏点击计数
  popupsClosed: 0,          // Stage 1 已关闭弹窗数
  letterDelivered: false,   // 终章信件是否已交付
  hasSaved: false           // 是否存在 localStorage 存档
};
```

### 3.1 状态持久化策略（localStorage）
每次状态变更时，将 `GlobalStateManager` 的核心字段序列化存入 `localStorage`（key: `stardew_gundam_save`）。

**保存时机**：
- Stage 1 弹窗全部关闭后
- Stage 2 每次物品收集后
- Stage 2 四季切换后
- Stage 3 信件阅读后

**加载时机**：
- 页面初始化时检查 `localStorage`，若存在存档且用户未主动清除，弹窗询问："检测到存档，是否继续上次的冒险？"（提供"继续"和"重新开始"两个按钮）。超时 10 秒未选择则自动继续。

**清除时机**：
- 用户完成 Stage 3 全部流程后，自动清除存档（给访客一个干净的结束）。
- 在页面角落提供"重置存档"小按钮，仅在开发者模式下可见或通过 Konami Code 触发。

### 3.2 核心状态流转机制
1. **初始化**：加载 `Stage 1: Office`。若 localStorage 有存档则先弹询问窗。
2. **解锁条件 1**：点掉 3 个工位弹窗 -> 浮现希望气泡 -> 点击气泡触发春风特效 -> 进入 `Stage 2: Seasons`（默认春季）。
3. **切换机制**：在 `Stage 2` 中，访客通过鼠标滚轮（Scroll Up = 下一季，Scroll Down = 上一季）按 **春 → 夏 → 秋 → 冬 → 春** 循环，在全屏平滑淡入淡出之间切换。滚轮事件需加防抖（debounce 800ms），防止快速滚动导致多次切换。
4. **解锁条件 2**：在四个季节场景中分别完成物品拾取，全部点亮底部 Inventory 物品栏（即 `inventory` 全部为 `true`）-> 自动触发 1.5 秒黑屏渐变，切换至 `Stage 3: Ending`。

---

## 四、 页面各阶段(Stage)详细拆解

### 4.1 Stage 1：工位受难记 (Office Nightmare)

**布局与元素**：
- 全屏背景挂载 `assets/bg_office.png`（王高达已烘焙至背景图中央，不需要独立 NPC 图像标签）。
- 屏幕中央偏上预留弹窗容器区域。

**交互逻辑**：
1. **弹窗轰炸**：页面加载后，依次弹出 3 个木纹边框警告窗口，间隔约 800ms（给用户消化时间）。每个弹窗出现在屏幕的随机位置，但通过碰撞检测确保互不重叠（弹窗尺寸约 400×200px，坐标范围为视口 10%-70% 区域）。
   - 弹窗 1："🔴 紧急Bug — 生产环境崩溃，立刻回滚！"
   - 弹窗 2："📋 老板的需求 — 这个需求很简单，下班前给个方案"
   - 弹窗 3："📅 会议邀请 — 下午6:00 对齐颗粒度，已重复：每天"
   - 每个弹窗均为 `box_dialog.png` 背景 + 文字 + 右上角红色的"X"关闭按钮。
2. **手动消除**：点击弹窗的"X"将其关闭，带有缩小消失的 CSS 过渡动画（`transform: scale(0); opacity: 0; transition: 0.3s`）。
3. **气泡浮现**：3 个弹窗全部消除后，屏幕中央偏上位置以 `ease-out` 弹跳动画淡入一个绿色嫩芽气泡图标（CSS 绘制或专用素材）。气泡带有呼吸灯效果（`@keyframes breathe { 0%, 100% { transform: scale(1); } 50% { transform: scale(1.08); } }`）。
4. **转场触发**：点击气泡，触发全屏白色光晕扩散特效（约 800ms），过渡至春季场景。

### 4.2 Stage 2：四季流转与内容探索 (Four Seasons Loop)

底部常驻 **"物品栏 (Inventory Bar)"**。包含 4 个格子，置于屏幕底部中央，半透明深色底栏（高约 60px），每格 48×48px，默认显示虚线框，已收集的格子显示对应道具图标并带金色光晕。

**NPC 对话触发机制**：
- 每个季节场景中，点击 NPC 图像（`wang_{season}.png`）即触发对话。
- 对话内容显示在屏幕下方的 `box_dialog.png` 对话框面板中（宽度约 70vw，高度自适应，定位于底部物品栏上方）。
- 对话文本采用**打字机逐字显示效果**（每个字符间隔约 50ms），配合"哒哒"打字音效（可选）。点击对话框任意位置可跳过打字机动画，直接显示完整文本。
- 如果该季节对话已触发过，点击 NPC 显示缩略版问候语而非完整打字机文本。
- **道具热区与 NPC 热区为独立 DOM 元素**，道具热区 z-index 高于 NPC，避免秋季捉迷藏时误触 NPC。

#### 四季场景交互矩阵规范（核心配置单）：

| 季节模块 | 背景与NPC资源配置 | 核心称号（对话框打字机文本） | 道具资产与交互逻辑 |
| :--- | :--- | :--- | :--- |
| **春季**<br>`SPRING` | 背景：`bg_spring.png`<br>NPC：`wang_spring.png` | "哟，被你撞见了。行吧，我摊牌——**【鹈鹕镇拾荒总教头】**这名字听着威风，翻译过来就是'职业翻垃圾桶的'。别人在会议室里对齐颗粒度，我在小巷子里翻外卖盒；别人年终总结写 OKR，我的年终总结是今年翻出了三把叉子两个贝壳。但说真的，好东西往往就藏在别人丢掉的地方。你往那边垃圾桶瞧瞧，好像有点什么。" | **可见道具**：`icon_item1.png`（绝对定位覆盖在环境垃圾桶上方）。<br>**点击逻辑**：点击后图标缩放消失（`scale: 0` + `opacity: 0`，0.3s 过渡），底部物品栏第一格亮起并渲染该图标（金色光晕动画）。 |
| **夏季**<br>`SUMMER` | 背景：`bg_summer.png`<br>NPC：`wang_summer.png` | "热死了吧？来，给你看看我的荣誉称号——**【全年无休的钓鱼机器】**。翻译：没有双休、没有调休、在这坐了八小时桶里还是空的。以前当社畜的时候天天盼周末，现在倒好，我自己主动周末也来。但你知道吗，我觉得这样挺好。至少鱼不会半夜钉我，湖水不会让我写周报。等待这件事本身，就已经是答案了。" | **可见道具**：`icon_item2.png`（绝对定位覆盖在环境鱼竿上方）。<br>**点击逻辑**：同上，图标消失，底部物品栏第二格点亮。 |
| **秋季**<br>`AUTUMN` | 背景：`bg_autumn.png`<br>NPC：`wang_autumn.png` | "……你想笑就笑吧。对，**【紫色短裤鉴赏家】**，你没看错。连我妈都发消息问我是不是转行做服装了。事情是这样的——村长刘易斯把他那条著名的紫色短裤落在玛妮家里了，别问我怎么知道的，问就是全镇都知道。总之，我现在是鹈鹕镇唯一一个'村长裤衩行为学'的田野调查员。说出去我自己都觉得离谱，但离谱的日子，至少不无聊。" | **可见道具（捉迷藏）**：`icon_item3.png`。<br>**点击逻辑**：<br>1. *第一次点击*：道具瞬间移动到屏幕另一随机绝对定位点（排除 NPC 所在区域 200px 半径范围），带短暂抖动动画提示"没抓住"。<br>2. *第二次点击*：道具从地图消失，底部物品栏第三格点亮。<br>**注意**：道具热区 z-index 设为 100，NPC 热区 z-index 设为 50，确保即使道具在 NPC 上方也能被优先捕获。 |
| **冬季**<br>`WINTER` | 背景：`bg_winter.png`<br>NPC：`wang_winter.png` | "冷吧？过来烤烤。**【冬日酸菜培育专家】**——这名字唬人吧？真相是我春天种了一地白菜，浇水浇了大半个月，眼看能收了……换季了。一夜之间全烂地里，比我年度绩效还惨。后来想通了，烂都烂了，不如就叫它酸菜。换个名字，至少能笑着跟人介绍。那边枯草堆里好像还剩了点什么。" | **可见道具**：`icon_item4.png`（绝对定位覆盖在枯草/镰刀上方）。<br>**点击逻辑**：点击后图标消失，底部物品栏第四格点亮。 |

*注：点击场景中可见道具时，需应用 `filter: drop-shadow(0 0 8px #ffd700)` 的高亮发光效果反馈，持续 200ms。*

**NPC 二次点击问候语**（该季节对话已触发后，点击 NPC 显示简短版）：
- 春季："今天翻到什么好东西没？"
- 夏季："鱼没钓着，但人晒得挺均匀。"
- 秋季："村长今天穿的什么颜色……算了当我没问。"
- 冬季："这一季的菜，赶在换季前收了吗？"

### 4.3 Stage 3：终章彩蛋与全景交付 (The Grand Finale)

**背景**：全屏挂载 `bg_final.png`（晴空农场）。

**触发条件**：4 个物品全部集齐 -> 自动 1.5s 黑屏过渡至 Stage 3。

**严格交互流**：

1. **交付物品**：进入 Stage 3 后，NPC `wang_final.png` 立于屏幕中央。四个收集到的物品图标从屏幕底部物品栏位置依次飞入 NPC 手中（CSS `@keyframes` 路径动画，每个间隔 300ms）。动画结束后弹出对话框："谢谢你帮我找回了这些记忆。作为回报，我送你一封信吧。"

2. **随机信件**：对话完毕（用户点击对话框关闭后），屏幕正中弹出信纸面板（复古羊皮纸风格 CSS 绘制，或 `box_dialog.png` 变体），利用 JS `Math.random()` 从 **10 封预设信件库**中随机抽取一封显示。信件内容以打字机效果逐字呈现，约 30 字/秒。访客阅读完毕后点击右下角的"折叠信纸"按钮关闭。

3. **视觉提示 — 再次点击引导**：信件关闭后，NPC `wang_final.png` 周围出现**柔和的脉动光晕**（`@keyframes pulse-glow`，金色光圈以 2s 周期明灭），持续吸引用户注意。同时 NPC 上方浮现小气泡文字 "再点我一下？"（每 3s 淡入淡出一次）。用户点击后进入下一步。

4. **社交面板**：点击王高达后，弹出一个人物状态栏面板（星露谷风格状态卡），展示：
   - 头像：`wang_final.png`
   - 姓名：王高达
   - 职业：AI 产品经理 / 鹈鹕镇荣誉居民
   - 技能条（像素进度条）：需求分析、原型设计、数据洞察、跨团队沟通
   - 社交链接：GitHub、邮箱、简历 PDF 等（以像素图标按钮形式排列）

---

## 五、 10 封预设"爷爷的信"文案库 (JS 预埋数组)

> **设计说明**：以下信件致敬了《星露谷物语》中爷爷在第三年降临评测农场的经典桥段。在游戏中，玩家扮演的角色继承了爷爷留下的农场，而爷爷的灵魂会在关键时刻回来看望你。在本网站中，"爷爷"作为一位经历过生活磨砺的长者，给身为打工人的玩家写下这些信——他理解你加班到深夜的疲惫，理解你在会议室里的沉默，也理解你偶尔抬头看窗外时那一瞬间的出神。

1. "孩子，今天下班的时候，你有没有抬头看看天色？爷爷种了一辈子地，最明白一件事——土地不会因为你盯着它看就长得更快。代码也不会因为你再多熬一小时就突然跑通。回家吧，洗个热水澡，晒晒太阳。你那些天大的难题，在明天的晨光里，会变得比今晚小一些。"

2. "他们说这个时代，年轻人得像永动机一样不停转动。别信。你看鹈鹕镇的冬天，万物枯萎、白雪覆盖，连最勤快的农夫都缩在火炉边打盹。可春天来了，种子照样发芽。记住：敢于停下来，也是一种了不起的勇气。你不是机器，你的疲惫值得被认真对待。"

3. "听说你最近在为一个方案焦头烂额。孩子，你知道鹈鹕镇的酿酒流为什么那么强吗？不是投入多，是发酵时间长。好果实放下去，第一年看不出什么，第二年银星，第三年金星。你的职业成长也是一样，别因为现在手里的东西不够闪亮就着急。你在扎根，别嫌慢。"

4. "我年轻时也当过牛马。在流水线上拧过螺丝，在账本前熬坏过眼睛。后来我才明白——种地也好，打工也罢，最重要不是把活干完，而是别把自己干丢了。偶尔在工位上发发呆，想想今晚想吃什么，想想周末要不要去河边走走。你首先是你自己，然后才是某家公司的某号员工。"

5. "鹈鹕镇的皮埃尔杂货店周三不开门，全镇的人都理解他。可是你周末回一条工作消息，同事们觉得天经地义。孩子，爷爷不是教你不努力，是想让你明白——你有权利守住你的边界。真正好的团队，永远容得下一个会休息的人。"

6. "上次你在电话里说觉得自己一事无成。傻孩子。我在这片土地上见过太多人了——有人追求星辰大海，有人只想守好炕头。没有谁比谁更正确。你每次踩点赶上的地铁、每个坚持练完的 demo、每一次在崩溃边缘又把自己拽回来的夜晚——这些不是什么了不起的成就，但它们是你在很认真地活着的证据。"

7. "职场上有人说你不够 aggressive，说你太安静、太温和、太不像一个'狠角色'。别理他们。爷爷告诉你一个秘密：整个星露谷最强的不是矿洞里最凶的怪物，是每天雷打不动给全村人送信的邮差。温柔而坚定地做事，不必声张，时间会替你说话的。"

8. "你攒的那些调休，什么时候才舍得用？爷爷认识的很多老人，走的时候最大的遗憾不是少赚了多少钱，是没能在身体还好的时候多去几个地方、多见几个人。孩子，你可以热爱你的工作，但别让它把你所有的清晨都变成打卡倒计时。去看看海，哪怕只是坐一下午。"

9. "被裁了？项目黄了？述职搞砸了？没关系。你知道农夫最羡慕谁吗？不是旁边那块地产量最高的邻居，是那个今年颗粒无收、明年依然笑着播种的傻子。人生不是一场绩效评估，它是一片你可以反复翻土、重新下种的土地。每一年都是第一年。"

10. "你终于走到了这里。看看你的背包——它装过深夜的泡面、通勤路上的耳机、老板画的大饼、同事推过来的锅。东西不少了，对吗？但你终究没有扔掉它，你还在走。这就是最了不起的事。接下来的路，不管往哪个方向，爷爷只想告诉你：**为自己活。哪怕只是一小部分时间，也要真正为自己活。** 我把这座农场留给你，不是让你继续当牛马的。是让你记得——总有一个地方，你可以自由地决定今天要种什么。"

---

## 六、 音效设计（可选增强项）

星露谷的沉浸感很大程度来自音乐和音效。建议后续迭代加入以下音频资源：

| 音效场景 | 推荐风格 | 触发时机 |
| :--- | :--- | :--- |
| 鼠标悬停（hover） | 轻柔的木块敲击声 | 鼠标划过任何可交互元素 |
| 弹窗弹出 | 纸张翻动声 | Stage 1 弹窗依次出现 |
| 弹窗关闭 | 戳破气泡声 | 点击弹窗 X 按钮 |
| 道具拾取 | 经典星露谷 "pop" 音效 | 四季物品被点击收集 |
| 物品栏点亮 | 短促上扬的叮咚声 | 物品栏格子亮起瞬间 |
| 季节切换 | 风声过渡 | 滚轮切换季节时 |
| 转场特效 | 低沉的嗡鸣淡入淡出 | Stage 1→2, Stage 2→3 |
| 信件展开 | 羊皮纸沙沙声 | Stage 3 信件弹出 |
| 打字机 | 短促哒哒声循环 | 所有打字机文本展示期间 |

> 音频资源可用免费音效库（如 freesound.org）获取，或使用 Web Audio API 生成简单合成音效。建议使用 `<audio>` 标签预加载，并设置全局静音/取消静音按钮。

---

## 七、 加载与容错策略

### 7.1 加载动画
- 在 `<body>` 最外层覆盖一个 `#loading-screen` 全屏遮罩，显示星露谷风格的 "Loading..." 文字（像素字体 + 呼吸动画）。
- 使用 `window.onload` 事件触发加载画面淡出（500ms），确保所有背景图在展示前已就绪。
- 若 5 秒仍未加载完成，显示"资源加载缓慢，请刷新页面重试"提示。

### 7.2 容错处理
- 所有 `<img>` 标签绑定 `onerror` 事件，加载失败时替换为 CSS 绘制的占位图（如纯色方块 + 问号文字）。
- 背景图加载失败时，显示 CSS fallback 纯色背景（深棕色 `#2d1b0e`）。
- `cursor_stardew.png` 加载失败时，CSS cursor 回退为系统默认 `default` 指针而非浏览器默认箭头。
- localStorage 读写使用 `try-catch` 包裹，避免隐私模式下 quota 超限导致 JS 崩溃。

---

## 八、 附录：现有素材审计与补充建议

基于对 `assets/` 目录的实际扫描，以下为需要关注的问题及改进建议：

### 8.1 必须修复的问题

| 问题 | 现状 | 修复方案 |
| :--- | :--- | :--- |
| **光标图片过大** | `cursor_stardew.png` 当前尺寸 1040×1428px，远超光标可用尺寸 | 降采样至 **32×32px** 或 **64×64px**。浏览器对超大 cursor 图片支持极差，多数会直接忽略 |
| **道具图标格式不一致** | `icon_item1.jpg`、`icon_item2.jpg` 为 JPG 格式，**无透明通道** | 转为 PNG 并抠除背景。JPG 素材无法叠在场景上，会出现白色/杂色方块 |
| **终章 NPC 过小** | `wang_final.jpg` 仅 252×376px，且为 JPG 无透明通道 | 改为透明背景 PNG，高度建议 ≥ 500px，确保在大屏幕上不模糊 |
| **bg_final.png 缺失** | PRD 要求但素材中不存在 | 补充 16:9 晴空农场背景图（2752×1536 与其他背景一致） |
| **未识别文件** | `1.jpg`(899×470), `2.png`/`3.png`/`4.png`(2816×1472), `5.png`(1664×2516) 未在 PRD 中定义用途 | 确认用途后并入资产表或从目录移除，避免混淆 |

### 8.2 UI 流畅度优化建议

| 类别 | 建议 | 效果 |
| :--- | :--- | :--- |
| **NPC 尺寸统一** | 当前四季 NPC 高度：spring 1280px, summer 1280px, autumn 1280px, winter 996px。建议统一为 **1200px 高度**，保证四季切换时 NPC 不产生跳变 | 四季切换更流畅，视觉连贯 |
| **道具图标尺寸统一** | 当前 item1 1404×1279, item2 1383×1279, item3 1440×1316, item4 1260×1140。建议统一裁剪至 **128×128px**，物品栏内展示一致 | 物品栏格子整齐美观 |
| **对话框素材优化** | `box_dialog.png`(1260×520) 建议改为九宫格切图素材（提供 stretch zones），或确保 CSS `border-image` 拉伸后边角不变形 | 不同文本长度的对话框都能保持木纹边框质感 |
| **增加过渡帧素材** | 转场特效（Stage 1→2 的春风花瓣、Stage 2→3 的黑屏过渡）若有专用过渡动画素材会更惊艳。建议增加 1-2 张过渡遮罩图 | 提升转场仪式感 |
| **命名规范化** | `wang_final.jpg` 建议统一为 `.png` 格式并确保透明背景；`wang_office.jpg` 若 PRD 明确不单独使用，建议从目录移除或标记为备用 | 减少开发时的引用混淆 |
| **favicon** | 建议增加 `favicon.ico`（取自 `icon_item1.png` 缩小的像素图标），浏览器标签页展示 | 细节处体现品质 |
| **背景压缩** | 当前 5 张背景图合计约 40MB，加载可能过慢。建议使用 TinyPNG 无损压缩，目标单张 ≤ 2MB | 首屏加载速度显著提升 |

### 8.3 建议新增素材清单

| 素材 | 用途 | 规格建议 |
| :--- | :--- | :--- |
| `sprout_bubble.png` | Stage 1 绿色嫩芽气泡图标 | 64×64px，透明背景，带呼吸动画则更佳 |
| `letter_paper.png` | Stage 3 信件羊皮纸底纹 | 600×800px，半透明边缘，可 CSS 替代 |
| `ui_skillbar.png` | Stage 3 技能进度条底纹 | 200×12px 像素风进度条 |
| `icon_github.png` / `icon_email.png` / `icon_resume.png` | Stage 3 社交链接图标 | 32×32px 像素图标，星露谷配色 |
| `favicon.ico` | 浏览器标签页图标 | 32×32px，取自关键道具或主角头像 |
| `logo_title.png` | 网页标题装饰（可选） | 像素风"Stardew Gundam"标题字，400×100px |
